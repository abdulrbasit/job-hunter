# Browser-hunt company list.
# DATA CONTRACT: key names (companies / name / career_url / location) are frozen.
# Never rename or remove keys — add companies only.
#
# Each entry runs through the full career-page extraction ladder:
# ATS API → JSON-LD → Sitemap → Static HTML → Lightpanda → Playwright → Firecrawl
# The highest rung that returns jobs wins.
#
# Fields:
#   name        (required) Company display name
#   career_url  (required) Direct URL to the careers page
#   location    (optional) Location hint for filtering, e.g. "Berlin" or ""

companies: []

# Example:
# companies:
#   - name: "Stripe"
#     career_url: "https://stripe.com/jobs"
#     location: ""
#   - name: "N26"
#     career_url: "https://n26.com/en/careers"
#     location: "Berlin"
