"""Canonical LLM prompt text, one module per role.

Config-driven prompt assembly (merging job_hunter.yml values into a prompt)
stays in the calling module. Only static prompt/system text lives here.
"""
